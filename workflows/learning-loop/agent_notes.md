# Learning Loop — Agent Notes

Patterns this workflow has learned about its own operation. Updated by the learning loop
itself during validation runs.

<!-- Example entries:
- Discovery-type corrections rarely become patterns — they tend to be one-offs.
  Consider raising the threshold for discoveries to 3+.
- Pattern detection runs fastest when corrections.md has <50 entries.
-->
